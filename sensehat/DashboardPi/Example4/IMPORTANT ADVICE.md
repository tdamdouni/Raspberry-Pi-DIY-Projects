**Program needed to create the animation**

In order to create the animation, ffmpeg is need. In Raspbian, it should work running: sudo apt-get install ffmpeg

If you are using other OS, check https://ffmpeg.org
